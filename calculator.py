"""
For the video demo, we'll use a calculator as an example.
First, we'll start with function tests, then build a class Calculator to demonstrate
class-based testing and pytest fixtures.
"""

def add(a: float, b: float) -> float:
    return a + b


def multiply(a: float, b: float) -> float:
    return a * a
